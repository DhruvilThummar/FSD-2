## Create index.htmlfile which contains uname,pass and login button. after clicking submit buton it should be jump on savesession page. store uname and pass. after saving session,redirect to fetch page and read value. also put logout link. jump on delete session page  where sesssion is destroy and redirect to home page. 