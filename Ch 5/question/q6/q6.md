## devlop a webapp yusing node.js and express.js with the following requirments. 
- **2** create an html form that takes a number, on submmition if input is empty then display "plese enter a number" and if input is not a number then display "invalid input".
- **2** if a proper number then display even number if number is even else odd number also provide a link to go back.
- **3** use 2 files html and js.