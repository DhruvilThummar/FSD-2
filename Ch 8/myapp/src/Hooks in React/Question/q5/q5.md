# Create a React app containing a form with the following fields:
- Full Name (`fname`)
- Email (`email`)
- Password (`pass`)
- Message (`msg` - text area)
- Gender (Radio buttons)
- City (Dropdown list)

Use the `useState` hook to capture the inputs, and display the submitted values in an alert box.
