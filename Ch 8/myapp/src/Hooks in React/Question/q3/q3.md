# Question 3

---

- Add 3 buttons: "Change Text", "Change Color", and "Hide/Show".
- Add an `<h1>` heading "LJU" in red color, and an `<h2>` tag with "ReactJS".
- Clicking the "Change Text" button should toggle the heading text between "LJU" and "Welcome Students" (and vice versa).
- Clicking the "Change Color" button should change the text color to blue.
- Initially, the "ReactJS" text should be visible. Clicking the "Hide/Show" button should toggle its visibility, changing the button text between "Hide" and "Show".
