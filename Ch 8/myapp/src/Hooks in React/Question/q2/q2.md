# Create a program to build a React app with buttons to increment and decrement a number:
- Clicking the respective button should change the count.
- The number should only increment if it is less than 10.
- The number should only decrement if it is greater than 0.
