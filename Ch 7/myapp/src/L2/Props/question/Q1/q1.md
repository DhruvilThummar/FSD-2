# Create a React application using props to display student details.

## Requirements
1. Create a parent component `Prop1.jsx`.
2. Pass data (student `name`, `rollno`, and `marks`) to a child component `Prop2.jsx`.
3. In the child component, display the student details, increase the marks by 1, and display the updated marks.
