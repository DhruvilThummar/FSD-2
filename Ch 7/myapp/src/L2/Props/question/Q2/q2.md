# Create a react application using props to pass an object from one to unother component.
- 1. Create a parent component `Example1.jsx`. 
- 2. create an object name `Deatils` containing student name university name.
- 3. pass the deatils object to the child component `exmple1.jsx` using props.
- 4. in a child component resived the object and displye student and uni name in the form of `"My name is xyz.I am studing in LJU"`