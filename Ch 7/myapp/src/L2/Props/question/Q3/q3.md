# Build a React application that displays a list of products.
1. Create a `ProductCard` component to show details such as `title`, `price`, `rating`, and `image` of products.
2. Create a `ProductList` component to store the product data and display multiple `ProductCard` components using `map`.
