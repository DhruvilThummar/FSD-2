# Create a component that contains an array of fruit objects, each having `itemId`, `itemName`, `price`, and `availability`. Use `filter` and `map` to:
1. Display only the available fruits.
2. Render each available fruit's name in an `<h1>` tag.
