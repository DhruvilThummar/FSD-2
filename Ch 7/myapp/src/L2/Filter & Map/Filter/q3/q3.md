# Create a React component that contains an array of student objects, each having `id`, `name`, and `std` (standard). Perform the following tasks using `filter` and `map`:
1. Display only the students whose standard (`std`) is 5, and increase their standard by 1.
2. Display the student's name and their updated standard.
