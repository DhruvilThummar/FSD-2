# Create a React component named `StudentCard` in a React Vite project. It should display the following details:
- Student Name, Enrollment Number, and Course.
- Pass these values as JavaScript expressions.
