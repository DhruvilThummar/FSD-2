# Create a parent component named `Hospital.jsx`. Inside it, create an object named `patient` containing the patient's name and the doctor's name.
1. Pass the `patient` object to a child component named `Appointment.jsx` using props.
2. In `Appointment.jsx`, display the patient and doctor names.
3. Create a form with a "Confirm Appointment" button.
4. Handle the `onSubmit` event: when submitted, display an alert message saying "Appointment confirmed for [Patient Name]" and prevent the default page reload.
