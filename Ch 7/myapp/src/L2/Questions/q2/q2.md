# Create a React component named `MyProfile` in a React Vite project.
The component should create a simple Profile UI with:
- A heading styled using inline CSS.
- A paragraph describing the profile.
- A list of skills.
- One image loaded from the `assets` folder.
- A button.
- Use external CSS as needed for styling.
