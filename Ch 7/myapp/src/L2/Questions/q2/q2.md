# Create a rect component named 'My Profile ' in a react vite project. the component should create a simple profile ui heading(inline css ),paregraph , list of skills , one image from assests folder and one boutton. use also external css as per need.
